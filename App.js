const heading = React.createElement('div', { id: 'head', className: 'heading' }, "Hello Ankit Saini");

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(heading);

console.log('Hello world!');